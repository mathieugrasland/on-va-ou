rules_version = '2';
// 🔐 Firestore Security Rules - Version 1.1.0 (Temporaire pour debug)
// Dernière modification: 2025-08-27
// TODO: Ajouter règles pour les groupes d'amis et recommandations de bars

service cloud.firestore {
  match /databases/{database}/documents {
    // Règles temporaires plus permissives pour tester l'inscription
    match /users/{userId} {
      // Permettre l'écriture pour les utilisateurs authentifiés (temporaire)
      allow read, write: if request.auth != null;
    }
    
    // Règles pour les futures collections (bars, groupes, etc.)
    match /bars/{barId} {
      // Lecture publique des bars, écriture pour les admins seulement
      allow read: if true;
      allow write: if false; // À modifier plus tard selon vos besoins
    }
    
    match /groups/{groupId} {
      // Les membres du groupe peuvent lire et écrire
      allow read, write: if request.auth != null && 
        request.auth.uid in resource.data.members;
    }
    
    // Bloquer tout autre accès
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
