{
  "name": "on-va-ou",
  "version": "1.0.0",
  "description": "Application web pour trouver des bars optimalement situés entre amis",
  "keywords": [
    "firebase",
    "javascript",
    "geolocation",
    "bars",
    "friends",
    "maps",
    "pwa"
  ],
  "author": "On va où ? Team",
  "license": "MIT",
  "homepage": "https://on-va-ou-app.web.app",
  "repository": {
    "type": "git",
    "url": "https://github.com/mathieugrasland/on-va-ou.git"
  },
  "bugs": {
    "url": "https://github.com/mathieugrasland/on-va-ou/issues"
  },
  "scripts": {
    "dev": "firebase serve",
    "deploy": "firebase deploy",
    "deploy:hosting": "firebase deploy --only hosting",
    "deploy:functions": "firebase deploy --only functions",
    "deploy:rules": "firebase deploy --only firestore:rules",
    "serve:dist": "firebase serve --only hosting",
    "logs": "firebase functions:log"
  },
  "devDependencies": {
    "firebase-tools": "^12.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ],
  "firebase": {
    "projects": {
      "default": "on-va-ou-app"
    }
  }
}
