{
  "name": "on-va-ou",
  "version": "1.0.0",
  "description": "Application web pour trouver des bars optimalement situés entre amis",
  "keywords": [
    "firebase",
    "javascript",
    "geolocation",
    "bars",
    "friends",
    "maps",
    "pwa"
  ],
  "author": "On va où ? Team",
  "license": "MIT",
  "homepage": "https://on-va-ou.web.app",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/on-va-ou.git"
  },
  "bugs": {
    "url": "https://github.com/your-username/on-va-ou/issues"
  },
  "scripts": {
    "dev": "firebase serve",
    "build": "node scripts/build.js",
    "deploy": "firebase deploy",
    "deploy:hosting": "firebase deploy --only hosting",
    "deploy:functions": "firebase deploy --only functions",
    "test": "echo \"Tests à implémenter\" && exit 0",
    "lint": "echo \"Linting à implémenter\" && exit 0",
    "clean": "rimraf dist",
    "serve:dist": "firebase serve --only hosting",
    "logs": "firebase functions:log"
  },
  "devDependencies": {
    "firebase-tools": "^12.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ],
  "firebase": {
    "projects": {
      "default": "on-va-ou-prod"
    }
  }
}
